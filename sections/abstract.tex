\begin{abstract}
Temporal knowledge graphs receive facts whose triples may be plausible even when their timestamps, entity roles, or supporting context are not. AnoT detects such updates with an interpretable rule graph learned from historical facts, but a graph of normal regularities alone gives limited evidence for different error mechanisms. We present \method, an error-aware extension of rule-graph anomaly detection. The method retains AnoT's normal rule graph and adds three type-specific evidence channels: role-consistency evidence for conceptual errors, contrastive temporal-gap rules for time errors, and train-only support rules for missing errors. A typed candidate--evidence--validation--scoring workflow records provenance for every proposed rule, verifies its evidence deterministically, and produces explanations only from accepted artifacts. On ICEWS14s, \method improves the reproduced AnoT baseline from 0.806/0.790/0.910 to 0.851/0.829/0.969 for conceptual errors and from 0.672/0.672/0.788 to 0.779/0.775/0.866 for time errors in Precision/\fbeta/AUC. For missing errors, it improves Precision and AUC from 0.726/0.729 to 0.751/0.822, while \fbeta remains sensitive to calibration. The implementation records the input evidence, selected calibration path, and final score for each evaluated fact.
\end{abstract}
